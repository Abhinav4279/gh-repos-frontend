export interface User {
  name?: string;
  avatar_url?: string;
  location?: string;
  bio?: string;
  twitter?: string;
  blog?: string;
  html_url?: string;
}

//name, avatar_url, location, bio, twitter, blog